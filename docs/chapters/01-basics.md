# Глава 1: Основы LLM

[🏠 На главную](../README.md) | [📑 Оглавление](../TOC.md) | [➡️ Следующая глава](./02-integration.md)

---

## Что такое LLM

**LLM (Large Language Model)** — это большая языковая модель, обученная на огромных объемах текстовых данных для понимания и генерации естественного языка. Современные LLM, такие как GPT-4, Claude 3 или Llama 3, содержат миллиарды параметров и способны выполнять широкий спектр задач: от ответов на вопросы до генерации кода и анализа данных.

### Как работает LLM

На базовом уровне LLM:
1. **Принимает входной текст** (prompt) в виде последовательности токенов
2. **Обрабатывает контекст** через множество слоев нейронной сети (transformer)
3. **Генерирует ответ** токен за токеном, предсказывая следующий наиболее вероятный токен

```
Вход: "Напиши функцию на TypeScript для"
          ↓
    [Transformer Network]
          ↓
Выход: "суммирования массива чисел: function sum(arr: number[]): number { ... }"
```

### Ключевые характеристики

- **Контекстное понимание**: модель анализирует весь контекст для генерации релевантного ответа
- **Few-shot learning**: может адаптироваться к новым задачам на основе нескольких примеров
- **Многозадачность**: одна модель может выполнять разные типы задач без дополнительного обучения
- **Генеративность**: создает новый контент, а не просто выбирает из заранее заданных ответов

---

## Токены и токенизация

### Что такое токен

**Токен** — это базовая единица текста, которую обрабатывает модель. Токен может быть словом, частью слова, символом или пробелом. Токенизация — это процесс разбиения текста на токены.

### Примеры токенизации

```typescript
// Пример 1: Простые слова
"Hello world" → ["Hello", " world"] // 2 токена

// Пример 2: Сложные слова
"TypeScript" → ["Type", "Script"] // 2 токена

// Пример 3: Русский язык (обычно больше токенов)
"привет мир" → ["при", "вет", " мир"] // 3 токена

// Пример 4: Код
"const x = 10;" → ["const", " x", " =", " 10", ";"] // 5 токенов
```

### Важные моменты

1. **Язык имеет значение**: русский и другие не-английские языки обычно требуют больше токенов
2. **Символы и форматирование**: пробелы, переносы строк — тоже токены
3. **Специальные токены**: модели используют специальные токены для обозначения начала/конца текста

### Подсчет токенов

Разные модели используют разные токенизаторы:

```typescript
// OpenAI (tiktoken)
import { encoding_for_model } from 'tiktoken';

const encoding = encoding_for_model('gpt-4');
const tokens = encoding.encode('Hello, world!');
console.log(tokens.length); // ~4 токена

// Альтернатива: использовать API для подсчета
import OpenAI from 'openai';

const openai = new OpenAI({ apiKey: process.env.OPENAI_API_KEY });

// Примерная формула: 1 токен ≈ 4 символа в английском
// Для русского: 1 токен ≈ 2-3 символа
```

### Влияние на стоимость

Токены напрямую влияют на стоимость использования API:

| Модель | Цена за 1K входных токенов | Цена за 1K выходных токенов |
|--------|----------------------------|------------------------------|
| GPT-4 Turbo | $0.01 | $0.03 |
| GPT-3.5 Turbo | $0.0005 | $0.0015 |
| Claude 3 Opus | $0.015 | $0.075 |
| Claude 3 Sonnet | $0.003 | $0.015 |

**Совет**: Всегда учитывайте количество токенов при проектировании промптов!

---

## Контекстное окно

### Что такое контекстное окно

**Контекстное окно (context window)** — это максимальное количество токенов, которое модель может обработать за один раз. Это включает как входной промпт, так и сгенерированный ответ.

### Визуализация

```
┌─────────────────────────────────────────────────────────┐
│              Контекстное окно (128K токенов)            │
├───────────────────────────────────┬─────────────────────┤
│     Входной промпт (100K)         │  Ответ (28K)        │
├───────────────────────────────────┴─────────────────────┤
│  System Prompt │ User Messages │ Assistant │ Response  │
└─────────────────────────────────────────────────────────┘
```

См. также: [Диаграмма контекстного окна](../../assets/diagrams/context-window.svg)

### Размеры контекстных окон (2025)

| Модель | Контекстное окно | Рекомендуемое использование |
|--------|------------------|------------------------------|
| GPT-4 Turbo | 128K токенов | Длинные документы, код |
| GPT-3.5 Turbo | 16K токенов | Чаты, короткие задачи |
| Claude 3 Opus | 200K токенов | Анализ книг, больших кодовых баз |
| Claude 3 Sonnet | 200K токенов | Универсальное применение |
| Llama 3 | 8K-32K токенов | Локальное использование |
| Mistral Large | 32K токенов | Среднего размера документы |

### Управление контекстом

Когда контекст превышает лимит, необходимо:

1. **Обрезать старые сообщения** (sliding window)
2. **Суммаризировать историю**
3. **Использовать RAG** для релевантной информации
4. **Разбить на несколько запросов**

```typescript
interface Message {
  role: 'user' | 'assistant' | 'system';
  content: string;
  tokens?: number;
}

function manageContext(
  messages: Message[],
  maxTokens: number
): Message[] {
  let totalTokens = 0;
  const result: Message[] = [];
  
  // Всегда сохраняем system prompt
  const systemMessage = messages.find(m => m.role === 'system');
  if (systemMessage) {
    result.push(systemMessage);
    totalTokens += systemMessage.tokens || 0;
  }
  
  // Добавляем сообщения с конца (самые свежие)
  for (let i = messages.length - 1; i >= 0; i--) {
    const msg = messages[i];
    if (msg.role === 'system') continue;
    
    const msgTokens = msg.tokens || 0;
    if (totalTokens + msgTokens > maxTokens) break;
    
    result.unshift(msg);
    totalTokens += msgTokens;
  }
  
  return result;
}
```

### Стратегии оптимизации

1. **Сжатие промптов**
   ```typescript
   // Плохо: избыточность
   "Please write a function in TypeScript that will sum all numbers in an array"
   
   // Хорошо: краткость
   "Write a TS function to sum array numbers"
   ```

2. **Использование ссылок вместо копирования**
   ```typescript
   // Плохо: вставка всего документа
   const prompt = `Документ: ${fullDocument}\n\nВопрос: ${question}`;
   
   // Хорошо: векторный поиск + релевантные части (RAG)
   const relevantChunks = await searchRelevant(question);
   const prompt = `Контекст: ${relevantChunks.join('\n')}\n\nВопрос: ${question}`;
   ```

3. **Суммаризация истории**
   ```typescript
   async function summarizeHistory(messages: Message[]): Promise<string> {
     const summary = await llm.complete({
       messages: [
         { role: 'system', content: 'Кратко суммируй диалог в 2-3 предложениях' },
         ...messages
       ]
     });
     return summary;
   }
   ```

---

## Семейства моделей

### OpenAI GPT

**GPT (Generative Pre-trained Transformer)** — семейство моделей от OpenAI.

#### GPT-4 Turbo (рекомендуется в 2025)
- **Контекст**: 128K токенов
- **Особенности**: лучшее качество, vision API, JSON mode
- **Использование**: сложные задачи, код, анализ
- **Цена**: $0.01 / $0.03 за 1K токенов

```typescript
import OpenAI from 'openai';

const openai = new OpenAI({
  apiKey: process.env.OPENAI_API_KEY,
});

const response = await openai.chat.completions.create({
  model: 'gpt-4-turbo-preview',
  messages: [
    { role: 'user', content: 'Объясни React Suspense' }
  ],
  temperature: 0.7,
  max_tokens: 500,
});
```

#### GPT-3.5 Turbo
- **Контекст**: 16K токенов
- **Особенности**: быстрая, дешевая
- **Использование**: простые задачи, чаты
- **Цена**: $0.0005 / $0.0015 за 1K токенов

### Anthropic Claude

**Claude** — семейство моделей от Anthropic, известное безопасностью и длинным контекстом.

#### Claude 3 Opus (топовая модель)
- **Контекст**: 200K токенов
- **Особенности**: лучшее качество, длинный контекст
- **Использование**: сложные задачи, длинные документы

#### Claude 3 Sonnet (баланс)
- **Контекст**: 200K токенов
- **Особенности**: хорошее качество, умеренная цена
- **Использование**: универсальное применение

#### Claude 3 Haiku (быстрая)
- **Контекст**: 200K токенов
- **Особенности**: очень быстрая, дешевая
- **Использование**: простые задачи в реальном времени

```typescript
import Anthropic from '@anthropic-ai/sdk';

const anthropic = new Anthropic({
  apiKey: process.env.ANTHROPIC_API_KEY,
});

const response = await anthropic.messages.create({
  model: 'claude-3-sonnet-20240229',
  max_tokens: 1024,
  messages: [
    { role: 'user', content: 'Объясни замыкания в JS' }
  ],
});
```

### Meta Llama

**Llama** — открытые модели от Meta, доступные для локального запуска.

#### Llama 3 (2024-2025)
- **Размеры**: 8B, 70B параметров
- **Контекст**: 8K-32K токенов
- **Особенности**: open source, можно запустить локально
- **Использование**: приватность, кастомизация, zero-cost inference

```bash
# Запуск через Ollama
ollama run llama3

# Или через API
curl http://localhost:11434/api/generate -d '{
  "model": "llama3",
  "prompt": "Explain React hooks"
}'
```

### Mistral AI

**Mistral** — европейская компания, создающая эффективные модели.

#### Mistral Large
- **Контекст**: 32K токенов
- **Особенности**: многоязычность, JSON mode
- **Использование**: альтернатива GPT-4

#### Mixtral 8x7B
- **Архитектура**: Mixture of Experts (MoE)
- **Особенности**: open source, эффективная
- **Использование**: локальное использование, self-hosting

### Google Gemini

#### Gemini 1.5 Pro
- **Контекст**: 1M токенов (!)
- **Особенности**: multimodal (текст, изображения, видео)
- **Использование**: анализ очень больших документов

---

## Локальные и Edge модели

### Когда использовать локальные модели

**Преимущества:**
- ✅ Приватность — данные не покидают ваш сервер/устройство
- ✅ Zero cost для inference (только стоимость железа)
- ✅ Низкая latency (нет сетевых запросов)
- ✅ Полный контроль и кастомизация

**Недостатки:**
- ❌ Требуют мощное железо (GPU)
- ❌ Обычно хуже качество чем GPT-4/Claude
- ❌ Сложность deployment и maintenance

### Ollama

[Ollama](https://ollama.ai/) — простой способ запустить LLM локально.

```bash
# Установка
curl -fsSL https://ollama.ai/install.sh | sh

# Запуск модели
ollama run llama3

# Список доступных моделей
ollama list

# API запрос
curl http://localhost:11434/api/chat -d '{
  "model": "llama3",
  "messages": [
    { "role": "user", "content": "Hello!" }
  ],
  "stream": false
}'
```

### WebLLM

[WebLLM](https://webllm.mlc.ai/) — запуск LLM прямо в браузере через WebGPU.

```typescript
import * as webllm from '@mlc-ai/web-llm';

// Инициализация модели в браузере
const engine = await webllm.CreateMLCEngine('Llama-3-8B-Instruct-q4f32_1');

// Генерация
const response = await engine.chat.completions.create({
  messages: [{ role: 'user', content: 'Привет!' }],
});

console.log(response.choices[0].message.content);
```

**Преимущества WebLLM:**
- Работает полностью офлайн
- Максимальная приватность
- Не требует сервера

**Ограничения:**
- Требует современный браузер с WebGPU
- Загрузка модели (несколько GB)
- Медленнее облачных решений

### Transformers.js

[Transformers.js](https://huggingface.co/docs/transformers.js) — библиотека от Hugging Face для запуска моделей в Node.js и браузере.

```typescript
import { pipeline } from '@xenova/transformers';

// Text generation
const generator = await pipeline('text-generation', 'Xenova/gpt2');
const output = await generator('Once upon a time', {
  max_length: 50,
});

// Embeddings
const embedder = await pipeline('feature-extraction', 'Xenova/all-MiniLM-L6-v2');
const embeddings = await embedder('Текст для эмбеддинга', {
  pooling: 'mean',
  normalize: true,
});
```

---

## Сравнение моделей

| Модель | Контекст | Качество | Скорость | Цена | Приватность |
|--------|----------|----------|----------|------|-------------|
| GPT-4 Turbo | 128K | ⭐⭐⭐⭐⭐ | ⚡⚡⚡ | 💰💰💰 | ☁️ Cloud |
| Claude 3 Opus | 200K | ⭐⭐⭐⭐⭐ | ⚡⚡ | 💰💰💰💰 | ☁️ Cloud |
| Claude 3 Sonnet | 200K | ⭐⭐⭐⭐ | ⚡⚡⚡ | 💰💰 | ☁️ Cloud |
| GPT-3.5 Turbo | 16K | ⭐⭐⭐ | ⚡⚡⚡⚡⚡ | 💰 | ☁️ Cloud |
| Llama 3 70B | 8K | ⭐⭐⭐⭐ | ⚡⚡ | Free* | 🔒 Local |
| Mistral Large | 32K | ⭐⭐⭐⭐ | ⚡⚡⚡ | 💰💰 | ☁️/🔒 |

\* Бесплатно для inference, но требует GPU

---

## Полезные ссылки

### Документация моделей
- [OpenAI Platform](https://platform.openai.com/docs)
- [Anthropic Claude](https://docs.anthropic.com/)
- [Cohere Docs](https://docs.cohere.com/)
- [Mistral AI](https://docs.mistral.ai/)
- [Google AI Studio](https://ai.google.dev/)

### Локальные решения
- [Ollama](https://ollama.ai/)
- [llama.cpp](https://github.com/ggerganov/llama.cpp)
- [WebLLM](https://webllm.mlc.ai/)
- [Transformers.js](https://huggingface.co/docs/transformers.js)

### Инструменты
- [OpenAI Tokenizer](https://platform.openai.com/tokenizer)
- [Tiktoken (Python/JS)](https://github.com/openai/tiktoken)
- [Hugging Face Models](https://huggingface.co/models)

---

## Резюме главы

В этой главе вы узнали:
- ✅ Что такое LLM и как они работают
- ✅ Что такое токены и как они влияют на стоимость
- ✅ Что такое контекстное окно и как им управлять
- ✅ Какие существуют семейства моделей и их особенности
- ✅ Когда и как использовать локальные модели

### Что дальше?

В следующей главе мы перейдем к практике — научимся интегрировать LLM API в ваше фронтенд-приложение.

---

[🏠 На главную](../README.md) | [📑 Оглавление](../TOC.md) | [➡️ Глава 2: Интеграция через API](./02-integration.md)
